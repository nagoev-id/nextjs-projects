'use client';

import { Button, Card } from '@/components/ui';
import { JSX } from 'react';

const Component = (): JSX.Element => {
  return (
    <Card>
      <h1>Component</h1>
      <Button>This is a simple component.</Button>
    </Card>
  );
};

export default Component;